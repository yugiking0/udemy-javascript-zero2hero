const bill = 430;
let tip = 0;
if ((bill >= 50) & (bill <= 300)) {
  tip = bill * 0.15;
} else {
  tip = bill * 0.2;
}
const total = bill + tip;
console.log(
  `The bill was ${bill}, the Tip was ${tip}, and the total value was ${total}`
);
//40-08-48
//430-86-516
// Kiểm tra
